# @package _global_

defaults:
  - override /logger: csv

tags: ["DEBUG", "limo", "D_aug"]

trainer:
  max_epochs: 5

  limit_train_batches: 0.05
  limit_val_batches: 0.05
  limit_test_batches: 0.05

dataset:
  missions_csv: ${paths.root_dir}/limo/configs/dataset/missions_split_debug_cached.csv

seed: 12345
